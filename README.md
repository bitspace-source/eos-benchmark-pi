# eos-benchmark-pi
a compute benchmark of pi on eos-node

## Initialize

run per session:

```cleos wallet open``` 

```cleos wallet unlock```

run per chain:

```sh conpile&run.sh```

## Use

run 

```python3 benchmark.py [<iterations> [<difficulty> [<precition> ["<account>"]]]]```
